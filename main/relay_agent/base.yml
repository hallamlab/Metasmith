channels:
  - conda-forge
dependencies:
  - python=3.10
  - pyinstaller=6.11.1
  - pip
  - pip:
    # why does numpy only work when installed thorugh pip???
    # otherwise:
    # ImportError: liblapack.so.3: cannot open shared object file: No such file or directory
    - numpy==2.2.5
