# Agent Notes for Metasmith

## Environment

- Use the `msm_env` mamba environment to run Python, tests, and CLI commands:
  ```
  mamba run -n msm_env <command>
  # or: conda run -n msm_env <command>
  ```
