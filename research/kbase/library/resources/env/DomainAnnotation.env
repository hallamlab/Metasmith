# KBase module DomainAnnotation v1.0.10, built from
# https://github.com/kbaseapps/DomainAnnotation.git at 093b943ead242d24227978d1df0b713d067beb89.
# The tag is domainannotation.093b943ead242d24227978d1df0b713d067beb89 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:53bc419030c6820c297f89e93e9028e9b1123dd8bc4ea2e676c361c9e9a3aca5
