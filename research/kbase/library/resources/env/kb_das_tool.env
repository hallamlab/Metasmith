# KBase module kb_das_tool v1.0.7, built from
# https://github.com/kbaseapps/kb_das_tool at c70fdbdef91d5b6614dfd387fa127c1492769a8f.
# The tag is kb_das_tool.c70fdbdef91d5b6614dfd387fa127c1492769a8f -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:2bc31ff045c1a8094ea2246abfd9b80f0629ebc73dedcaf71ec39c00c2416a79
