# KBase module RAST_SDK v1.9.5, built from
# https://github.com/kbaseapps/RAST_SDK at 7171090d87fccc8b7ecf1a1d02398995dcc2dd45.
# The tag is rast_sdk.7171090d87fccc8b7ecf1a1d02398995dcc2dd45 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:1e1c35c467b100ae9bf63214cca84d63497add03f48fbd7a593216a04d9ee873
