# KBase module dan_hoppLighter v0.1.1, built from
# https://github.com/kbaseapps/kb_lighter at 222baf794d68837564761e08db5d0dd6cecf232a.
# The tag is dan_hopplighter.222baf7 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:ef23e517fd26e7a9a14537a5c34b56f4f7d23f2dc65c2e787415c30d9db3d807
