# KBase module kb_PRINSEQ v0.0.6, built from
# https://github.com/kbaseapps/kb_PRINSEQ/ at 8d686578cddaee4a9b7b5e3a642cf27f380fc00f.
# The tag is kb_prinseq.8d686578cddaee4a9b7b5e3a642cf27f380fc00f -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:c0a5606a40e317dcef74eb4f33c7bfda47957f53e444c83e49bfa442c0ae06bb
