# KBase module kb_DRAM v0.1.2, built from
# https://github.com/kbaseapps/kb_DRAM at 935498b6cc1a10df8a8405516141734b23cc5f15.
# The tag is kb_dram.935498b6cc1a10df8a8405516141734b23cc5f15 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:31e46e9030536683ecd3fd4255aabfd5ba53fd0b5c323b475d9559caf031b094
