# KBase module kb_staging_exporter v1.0.10, built from
# https://github.com/kbaseapps/kb_staging_exporter at be61abfb5686a145be36f42bc36a8d0a1ab57ca6.
# The tag is kb_staging_exporter.be61abfb5686a145be36f42bc36a8d0a1ab57ca6 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:17a15c914883fff99079c8c88688d667ab2e70619431314656e892543de1b30f
