# KBase module MEGAHIT v2.4.3, built from
# https://github.com/kbaseapps/kb_megahit at e2e6d557da8a92d8b9a6cb4893a2a7293a3068bf.
# The tag is megahit.e2e6d557da8a92d8b9a6cb4893a2a7293a3068bf -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:e993842bfe4c6093bb7b60aadc1a19bf8d5be3f28f819b16ab5bf25dc4bdd47c
