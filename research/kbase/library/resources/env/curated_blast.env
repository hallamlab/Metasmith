# KBase module curated_blast v0.0.3, built from
# https://github.com/OGalOz/curated_blast at 8a19a8ad23fad0840d7a3e3a4cf78833748d1e29.
# The tag is curated_blast.8a19a8ad23fad0840d7a3e3a4cf78833748d1e29 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:bbc92388968152e8e585fc18dda75d3f38e45c4a89d8c3dafdd428a302d7e52b
