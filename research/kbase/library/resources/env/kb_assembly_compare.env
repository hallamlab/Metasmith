# KBase module kb_assembly_compare v1.1.6, built from
# https://github.com/kbaseapps/kb_assembly_compare at 87b74d0dba8cb19dcbd3e7782d4f73a5eaeff76b.
# The tag is kb_assembly_compare.87b74d0dba8cb19dcbd3e7782d4f73a5eaeff76b -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:4cdb2554cbcfd1b4263cff2f9a2a7c98e3d091687fe81dd86a9b38e7af53e3eb
