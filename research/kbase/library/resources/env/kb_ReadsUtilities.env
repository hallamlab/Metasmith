# KBase module kb_ReadsUtilities v1.2.2, built from
# https://github.com/kbaseapps/kb_ReadsUtilities at de8955d0a9f373bff40882a02cd7742aa7560eea.
# The tag is kb_readsutilities.de8955d0a9f373bff40882a02cd7742aa7560eea -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:c072d5bcbc1c62c3334fcac9ba3be0d308f97f9e903d2e3e7d8d13aac0215615
