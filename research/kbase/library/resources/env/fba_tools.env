# KBase module fba_tools v2.2.1, built from
# https://github.com/kbaseapps/fba_tools at b083384ac00d4f9d7cb796a664ee3ffd017cf248.
# The tag is fba_tools.b083384ac00d4f9d7cb796a664ee3ffd017cf248 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:10ac5dbf080c373a5664b2be6e57dee1fec4eb169e313c3f5b060748027e1fd1
