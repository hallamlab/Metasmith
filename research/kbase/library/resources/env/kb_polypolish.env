# KBase module kb_polypolish v1.0.0, built from
# https://github.com/kbaseapps/kb_polypolish.git at 26e93df24642a15a43a1ebabbbc54261e871b9ee.
# The tag is kb_polypolish.26e93df24642a15a43a1ebabbbc54261e871b9ee -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:03c38fc70b831f4e8484fef852d33488c10718a8cb22342a7097cbc553efe575
