# KBase module MergeMetabolicAnnotations v1.2.11, built from
# https://github.com/jeffkimbrel/MergeMetabolicAnnotations at ec971d114d57942cef73dc2980c8faf48cea7afe.
# The tag is mergemetabolicannotations.ec971d114d57942cef73dc2980c8faf48cea7afe -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:4196c4fad9295ec7b45dea80ecb02b3dcca68c98cc252d3962282d0f684ffdd0
