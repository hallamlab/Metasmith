# KBase module kb_nanoplot v1.0.0, built from
# https://github.com/kbaseapps/kb_nanoplot at 16c1549098895d921401ffe9c3fab4bfb1625ea8.
# The tag is kb_nanoplot.16c1549 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:23c547df0e91380d8e61a4fb23f6c83f5973cb66f115b5162b7ae81a5c73b759
