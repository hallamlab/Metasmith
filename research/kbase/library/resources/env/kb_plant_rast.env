# KBase module kb_plant_rast v0.0.3, built from
# https://github.com/kbaseapps/kb_plant_rast at 45b3934211eca408bf3e083b8f4a6ec29843cd6a.
# The tag is kb_plant_rast.45b3934211eca408bf3e083b8f4a6ec29843cd6a -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:9a7661034366f8fd48475c8905c1af13471e25264877eb9c9d1399fe23d60159
