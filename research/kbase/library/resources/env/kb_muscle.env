# KBase module kb_muscle v1.1.2, built from
# https://github.com/kbaseapps/kb_muscle at f6cbe3df490aa7edc4788000e6964f601cb9c83a.
# The tag is kb_muscle.f6cbe3df490aa7edc4788000e6964f601cb9c83a -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:c40233da86d1f6973ddcfd069bbbe9dea30e386fe3f4144c448b7976f87ce02d
