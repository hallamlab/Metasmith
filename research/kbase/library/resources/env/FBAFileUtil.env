# KBase module FBAFileUtil v0.1.1, built from
# https://github.com/kbaseapps/FBAFileUtil at d07004d2b3710a43dbb1c17a0726d9a22ab35dce.
# The tag is fbafileutil.d07004d2b3710a43dbb1c17a0726d9a22ab35dce -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:f1662aca0cc8e2ed709f809d40cabddf208d03e8edc824a4b982f1bb5993d382
