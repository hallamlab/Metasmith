# KBase module MutualInformationAnalysisModule v1.0.0, built from
# https://github.com/zahmeeth/MutualInformationAnalysisModule at 4a36f36af2ab8cff4e54c9e7fc6dac1955bb5037.
# The tag is mutualinformationanalysismodule.4a36f36 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:f1d81c9268bff65997b55590b2e8296fd03810dafdb9b4fa089e2c3c46673b80
