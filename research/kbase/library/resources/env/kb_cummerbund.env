# KBase module kb_cummerbund v1.0.4, built from
# https://github.com/kbaseapps/kb_cummerbund at 50fe0c313af47686b20da1a5961a034664e3be20.
# The tag is kb_cummerbund.50fe0c313af47686b20da1a5961a034664e3be20 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:a7bc88a27efd9214d6e19f7fdefa9c6a3bf1116e0610a7f529f4c6bc05bcf4f6
