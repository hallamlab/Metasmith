# KBase module SetAPI v0.3.5, built from
# https://github.com/kbaseapps/SetAPI at 156c9258afb4e7dd5b1edb5731e1a7ea3399272b.
# The tag is setapi.156c9258afb4e7dd5b1edb5731e1a7ea3399272b -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:b3ffe8da37e0aad9420383337ae93a1e594fbc8ad8b1f881fdd9139bd89bc4fe
