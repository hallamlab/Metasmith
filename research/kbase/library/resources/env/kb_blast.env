# KBase module kb_blast v1.7.0, built from
# https://github.com/kbaseapps/kb_blast at 791f72df62105af2c74f436e8f3452c932e8db68.
# The tag is kb_blast.791f72df62105af2c74f436e8f3452c932e8db68 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:5e835df56c0f8f590905b5c1c23adf2c97193bcb5e68b984599553bcf9190a42
