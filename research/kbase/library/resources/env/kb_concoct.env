# KBase module kb_concoct v1.3.4, built from
# https://github.com/kbaseapps/kb_concoct at 936c9bfed7e07dd1ddcb76ac4fa775b73d60afbb.
# The tag is kb_concoct.936c9bfed7e07dd1ddcb76ac4fa775b73d60afbb -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:8480a8dc0d3a37d6a253eac7850375886f55a75a7de03336576add415368e056
