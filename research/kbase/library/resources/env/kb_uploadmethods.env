# KBase module kb_uploadmethods v1.0.57, built from
# https://github.com/kbaseapps/kb_uploadmethods at 5b9346463df88a422ff5d4f4cba421679f63c73f.
# The tag is kb_uploadmethods.5b9346463df88a422ff5d4f4cba421679f63c73f -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:c58cba621bef6106df758ce4f96fe212ebf6018ecdc72bed778aa3f57863d08a
