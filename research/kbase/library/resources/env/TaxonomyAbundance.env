# KBase module TaxonomyAbundance v1.0.0, built from
# https://github.com/kbaseapps/TaxonomyAbundance at da7a8968bb355e2abfc531001474e3b8104eb50f.
# The tag is taxonomyabundance.da7a8968bb355e2abfc531001474e3b8104eb50f -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:40491c1f4de909e4f549f957e37fd0e21b29d19f5ed572172e6dbf1fc970483e
