# KBase module kb_cufflinks v0.2.1, built from
# https://github.com/kbaseapps/kb_cufflinks at d2a31f34c14aa9536a3776e7a6f9c28687ad638c.
# The tag is kb_cufflinks.d2a31f34c14aa9536a3776e7a6f9c28687ad638c -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:0e4b34284aa65798df3e144dc19723cd4b579640a8000657154617a57b57d548
