# KBase module GenericsAPI v1.0.29, built from
# https://github.com/kbaseapps/GenericsAPI at 2d68b13be4bf7278817cbfef949777a03ea1bda7.
# The tag is genericsapi.2d68b13be4bf7278817cbfef949777a03ea1bda7 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:5de6623ef66d15c61bf4fd72d4c4dcfb1913ffdb46e47101df574a2dfe17d527
