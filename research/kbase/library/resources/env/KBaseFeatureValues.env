# KBase module KBaseFeatureValues v0.0.22, built from
# https://github.com/kbaseapps/FeatureValues at 6cdc50905a08883a53333c073abe1e1df7b3f97f.
# The tag is kbasefeaturevalues.6cdc50905a08883a53333c073abe1e1df7b3f97f -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:6d05af06fc241d05ba03132f897ceec469bf457c0190201b1a4d25f6f2b5efdf
