# KBase module plant_fba v1.2.0, built from
# https://github.com/kbaseapps/plant_fba at a8031aa0f4320c7cd96f78170d7644f696614f8d.
# The tag is plant_fba.a8031aa0f4320c7cd96f78170d7644f696614f8d -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:2a8aeceaa5e4270e07836db2bcde68e1049eaf97d2d1e3e216e94d151409f334
