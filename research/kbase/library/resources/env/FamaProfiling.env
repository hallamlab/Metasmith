# KBase module FamaProfiling v1.1.0, built from
# https://github.com/aekazakov/FamaProfiling at d9db15ea217e3be2aab65c356564a6d345b4f410.
# The tag is famaprofiling.d9db15ea217e3be2aab65c356564a6d345b4f410 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:b243bfd0073a050efa20dd35eb1621f9fa0d49cdb20714d3aba1b562f90ad16e
