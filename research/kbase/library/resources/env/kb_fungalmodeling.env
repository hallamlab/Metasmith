# KBase module kb_fungalmodeling v1.0.0, built from
# https://github.com/janakagithub/kb_fungalmodeling at 8e62fff663f130b40c7b55ce43b9b0110967ce8a.
# The tag is kb_fungalmodeling.8e62fff663f130b40c7b55ce43b9b0110967ce8a -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:df93ff26510e3c43cec5d8b10ccf19f89a1152a974eec8b1da1515cac681b1f8
