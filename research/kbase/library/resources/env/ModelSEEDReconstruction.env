# KBase module ModelSEEDReconstruction v2.0.0, built from
# https://github.com/ModelSEED/KB-ModelSEEDReconstruction at 061b8d25e7bc0527c4d4c2e72c4161869169117f.
# The tag is modelseedreconstruction.061b8d25e7bc0527c4d4c2e72c4161869169117f -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:cb5391247ec458492198f387adbf325daad615204ad687639da187e398bfafa9
