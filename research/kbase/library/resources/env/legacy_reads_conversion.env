# KBase module legacy_reads_conversion v0.0.2, built from
# https://github.com/kbaseapps/legacy_reads_conversion at 2c071c5ae32e83a94a1fc4980ac223deefb54fe4.
# The tag is legacy_reads_conversion.2c071c5ae32e83a94a1fc4980ac223deefb54fe4 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:94190d7cb71ac32be2839bc79d68778a32d78c5f12f6560291fc6bc2d415852e
