# KBase module SpeciesTreeBuilder v0.1.4, built from
# https://github.com/kbaseapps/SpeciesTreeBuilder at dce166f6d1673018a001b750c191b9a2deda0c71.
# The tag is speciestreebuilder.dce166f6d1673018a001b750c191b9a2deda0c71 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:37d9858430a74b4fe58207e2ce367c67df081b5578e0cd06fcac880770c29ff1
