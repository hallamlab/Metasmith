# KBase module genome_transform v1.2.2, built from
# https://github.com/kbaseapps/genome_transform at de453501e83775cd7e75cfc046eeac2a170e694a.
# The tag is genome_transform.de453501e83775cd7e75cfc046eeac2a170e694a -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:b4833db255451252e77dbd01e04be1a67dfb4b67d4b9b563b77d8a704a519485
