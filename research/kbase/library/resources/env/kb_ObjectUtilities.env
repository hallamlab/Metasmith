# KBase module kb_ObjectUtilities v1.4.1, built from
# https://github.com/kbaseapps/kb_ObjectUtilities at 4bbb44c391a5c3062cc66cb56573072b15c800e8.
# The tag is kb_objectutilities.4bbb44c391a5c3062cc66cb56573072b15c800e8 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:4d0b321e687a92a95f0fa6418bd683ce1ed902a45bb7e41e2b261bc6a5841d71
