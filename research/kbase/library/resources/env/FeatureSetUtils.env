# KBase module FeatureSetUtils v1.2.6, built from
# https://github.com/kbaseapps/FeatureSetUtils at e31f1e7de3ce34a595574e7edfa2a85944cd07a0.
# The tag is featuresetutils.e31f1e7de3ce34a595574e7edfa2a85944cd07a0 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:9f78642ef05ff42fc53117dafa94df15eb298e70f1dcae868075ba5f726421b8
