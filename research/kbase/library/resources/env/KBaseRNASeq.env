# KBase module KBaseRNASeq v1.0.5, built from
# https://github.com/kbase/KBaseRNASeq at 07a2978aea7cabf4decec09c2dc51be81e26f02d.
# The tag is kbasernaseq.07a2978 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:d31950f0019871e77dd319c6dbd1f2f7ebfd5b77d0a5d67da59c37768d87a9aa
