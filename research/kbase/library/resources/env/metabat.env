# KBase module metabat v2.3.0, built from
# https://gitlab.com/jfroula/kbase-metabat at caf4e3c0c415602f3e11f1473734a7b682c6fdfb.
# The tag is metabat.caf4e3c0c415602f3e11f1473734a7b682c6fdfb -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:f238145c1c22832b7a16098c0e066799a1f29b15625521e0f4498d6a7856904d
