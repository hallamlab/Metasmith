# KBase module AssemblyRAST v0.0.13, built from
# https://github.com/kbaseapps/ARAST_SDK at 056582c691c4df190110b059600d2dc2a3a8b80a.
# The tag is assemblyrast.056582c691c4df190110b059600d2dc2a3a8b80a -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:7777608fc46c4e55af6139325275f1a07ddbcbdc23eb48e73c9c2cbf45f6cb1b
