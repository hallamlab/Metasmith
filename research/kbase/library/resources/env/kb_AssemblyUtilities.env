# KBase module kb_AssemblyUtilities v1.2.0, built from
# https://github.com/kbaseapps/kb_AssemblyUtilities at 4304b6160f959fbc7302c6cf39799b5c37fd6ec6.
# The tag is kb_assemblyutilities.4304b6160f959fbc7302c6cf39799b5c37fd6ec6 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:29f0964a1f17b9770e3fd86fe60e75a26a12ca60eb504b69b707a26cb638c1d9
