# KBase module sample_uploader v1.1.1, built from
# https://github.com/kbaseapps/sample_uploader/ at 20e60df2dfa54b90ba25dad4110cea5d0123754e.
# The tag is sample_uploader.20e60df2dfa54b90ba25dad4110cea5d0123754e -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:214ca9ba95be625551de50e90c890d57fd9783ea081338a1b264ada470e06da3
