# KBase module GenomeProteomeComparison v0.0.8, built from
# https://github.com/kbaseapps/GenomeProteomeComparison at f96f62941820a793124a18fe6e655da33a562033.
# The tag is genomeproteomecomparison.f96f62941820a793124a18fe6e655da33a562033 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:13324a642e37dacb404663fa980f18d2bb7cb90570ef8f17067936ccebd64f72
