# KBase module NarrativeViewers v1.0.9, built from
# https://github.com/kbase/NarrativeViewers at dd1eeeba0ba1faacd6c3596a9413109aeb82e32e.
# The tag is narrativeviewers.dd1eeeba0ba1faacd6c3596a9413109aeb82e32e -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:144a2da2199b4bd2f813f0dc6a576eed8674415a11498b45027bc8c1af964cce
