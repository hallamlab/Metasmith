# KBase module Weka v0.0.1, built from
# https://github.com/mikacashman/Weka.git at 3b595e8b76fd8040a22ff36254f24e620b958da6.
# The tag is weka.3b595e8b76fd8040a22ff36254f24e620b958da6 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:00e60210524855c692f96c84c3ac4a8e2f53b5fff7c8424a45091587a3b0a9ee
