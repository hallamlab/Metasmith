# KBase module ProkkaAnnotation v3.2.1, built from
# https://github.com/kbaseapps/ProkkaAnnotation at 1967e07320b1898db0c6998bb4549b7f1187e5a8.
# The tag is prokkaannotation.1967e07320b1898db0c6998bb4549b7f1187e5a8 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:c31828d30a06586c8ad2510f26e9c7768bd3329495bd6a40077d54fbae1c5530
