# KBase module kb_hisat2 v1.1.4, built from
# https://github.com/kbaseapps/kb_hisat2 at 0ee63445183193878ebdf80ed8349d95ffc9a682.
# The tag is kb_hisat2.0ee63445183193878ebdf80ed8349d95ffc9a682 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:1c83517994dd948c60016669b3150eef896a0e920ca3396c56303a11bd269dce
