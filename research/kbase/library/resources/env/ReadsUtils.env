# KBase module ReadsUtils v1.0.1, built from
# https://github.com/kbaseapps/ReadsUtils at 04ed67ed6d5c72020009016220adb2db32e74ae8.
# The tag is readsutils.04ed67ed6d5c72020009016220adb2db32e74ae8 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:6fc74d3a73b9a1e2c12e4e83e7746273169baeaed5d0788680975016f74bd11d
