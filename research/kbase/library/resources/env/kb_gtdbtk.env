# KBase module kb_gtdbtk v1.4.1, built from
# https://github.com/kbaseapps/kb_gtdbtk at 99839a03cebee1110c6cd6d807dc448821837580.
# The tag is kb_gtdbtk.99839a03cebee1110c6cd6d807dc448821837580 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:07bf52b752e7a5e909305e40e374622cb70a3f29491d2e06dce0368690c7fede
