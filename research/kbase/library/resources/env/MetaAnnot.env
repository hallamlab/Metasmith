# KBase module MetaAnnot v1.5.1, built from
# https://gitlab.com/jfroula/kbase-metaAnnot at 62a658fe089f7aee2d340b894e8c092b435b4076.
# The tag is metaannot.62a658fe089f7aee2d340b894e8c092b435b4076 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:3354c42038c957fd2566ed28d19b02fc8013239c3e68a4784db0ae4ecd29e929
