# KBase module kb_meta_decoder v1.0.2, built from
# https://github.com/kbaseapps/kb_meta_decoder at 2c0fd8e0b7df5b9a564cd2e8deffd55c35ea1b4c.
# The tag is kb_meta_decoder.2c0fd8e0b7df5b9a564cd2e8deffd55c35ea1b4c -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:5671b0372ff268a5ce7d9118aa62e902d89fa0a5ef72b702e3aba12c6efe0b57
