# KBase module BBTools v0.5.0, built from
# https://github.com/kbaseapps/BBTools at cdfefe87f15639c14de2d7ad137920b892a8e35b.
# The tag is bbtools.cdfefe87f15639c14de2d7ad137920b892a8e35b -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:803cdb81705da31da3677768cb2aaf14b54b1a021a0305ab6e37cd7ecb1d6f94
