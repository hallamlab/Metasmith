# KBase module GenomeFileUtil v0.11.6, built from
# https://github.com/kbaseapps/GenomeFileUtil at c8c6948e0bebb9f0a6e95abe057af62b3ca57195.
# The tag is genomefileutil.c8c6948e0bebb9f0a6e95abe057af62b3ca57195 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:7a03983ccd51dc0258eb629a5c89a430f4e07e53fd0369961c3a661b19b5ac08
