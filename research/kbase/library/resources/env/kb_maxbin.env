# KBase module kb_maxbin v1.1.1, built from
# https://github.com/kbaseapps/kb_maxbin at 3574f0b2cc343f4f530bd52b86c8cf8b6b1427f3.
# The tag is kb_maxbin.3574f0b2cc343f4f530bd52b86c8cf8b6b1427f3 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:21d75177a92a9bcd2d504bac78ebcdc8df5d5e678df67b58346ae6455fbae057
