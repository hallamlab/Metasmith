# KBase module hipmer v2.2.4, built from
# https://github.com/kbaseapps/kbase_hipmer at 538ffad4a25b69ed119091b753bde6019193701b.
# The tag is hipmer.538ffad4a25b69ed119091b753bde6019193701b -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:1a9029a15dfc64769864898386f56aa46f44abc155604d630e03369bebc91c9c
