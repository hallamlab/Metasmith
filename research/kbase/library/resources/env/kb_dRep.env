# KBase module kb_dRep v1.0.0, built from
# https://github.com/kbaseapps/kb_dRep at 805c450d9fa4767567aa452ca1a744bb25c15813.
# The tag is kb_drep.805c450d9fa4767567aa452ca1a744bb25c15813 -- KBase embeds the
# module's git commit in the tag, so the tag is already immutable by
# construction. The digest below is that same image, resolved from the
# registry on 2026-09-04, because a digest is what this library pins by.
container: docker://dockerhub-prod.kbase.us/kbase@sha256:619cf8b2ddb3b55b31bbaaf92e7f074ce9fcec3b05b24f17c05da0c6974c753f
