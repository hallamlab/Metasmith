# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""ProkkaAnnotation/annotate_contigs -- Annotate Assembly and Re-annotate Genomes with Prokka - v1.14.5

KBase app   : ProkkaAnnotation/annotate_contigs (spec 3.2.1, active)
module      : ProkkaAnnotation
git         : https://github.com/kbaseapps/ProkkaAnnotation @ 1967e07320b1898db0c6998bb4549b7f1187e5a8
service     : ProkkaAnnotation.annotate

Annotate Assembly and Re-annotate Genomes with Prokka annotation pipeline.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study              = model.AddRequirement(lib.GetType("kbase::study"))
sample             = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
object_ref         = model.AddRequirement(lib.GetType("kbase::accepts_40"), parents={sample})
env                = model.AddRequirement(lib.GetType("env::ProkkaAnnotation.env"))
output_genome_name = model.AddProduct(lib.GetType("kbase::KBaseGenomes_Genome"))
report_            = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output_genome_name: context.Output(output_genome_name)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=object_ref,
    labels=[],
    resources=Resources(
        cpus=8,
        memory=Size.GB(16),
        duration=Duration(hours=12),
    ),
)
