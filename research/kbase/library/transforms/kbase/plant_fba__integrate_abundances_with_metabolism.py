# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""plant_fba/integrate_abundances_with_metabolism -- Integrate Abundances with Metabolism

KBase app   : plant_fba/integrate_abundances_with_metabolism (spec 1.2.0, active)
module      : plant_fba
git         : https://github.com/kbaseapps/plant_fba @ a8031aa0f4320c7cd96f78170d7644f696614f8d
service     : plant_fba.integrate_abundances_with_metabolism

Integrate gene abundances with a plant primary metabolic network

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study             = model.AddRequirement(lib.GetType("kbase::study"))
sample            = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
expression_matrix = model.AddRequirement(lib.GetType("kbase::KBaseFeatureValues_ExpressionMatrix"), parents={sample})
fba_model         = model.AddRequirement(lib.GetType("kbase::KBaseFBA_FBAModel"), parents={sample})
env               = model.AddRequirement(lib.GetType("env::plant_fba.env"))
reaction_matrix   = model.AddProduct(lib.GetType("kbase::KBaseMatrices_ReactionMatrix"))
report_           = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {reaction_matrix: context.Output(reaction_matrix)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=expression_matrix,
    labels=[],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
