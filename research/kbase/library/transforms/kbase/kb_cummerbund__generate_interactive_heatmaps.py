# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""kb_cummerbund/generate_interactive_heatmaps -- Create Differential Expression Matrix and HeatMap using Cuffdiff

KBase app   : kb_cummerbund/generate_interactive_heatmaps (spec 1.0.4, inactive)
module      : kb_cummerbund
git         : https://github.com/kbaseapps/kb_cummerbund @ 50fe0c313af47686b20da1a5961a034664e3be20
service     : kb_cummerbund.create_interactive_heatmap_de_genes_old

Compare a pair of conditions in RNA-seq expression data to identify
differentially expressed genes and view them in an interactive heatmap. This
app requires more than one condition and each condition should have at least
two replicates.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study          = model.AddRequirement(lib.GetType("kbase::study"))
sample         = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
ws_cuffdiff_id = model.AddRequirement(lib.GetType("kbase::KBaseRNASeq_RNASeqDifferentialExpression"), parents={sample})
env            = model.AddRequirement(lib.GetType("env::kb_cummerbund.env"))
report_        = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {report_: context.Output(report_)}
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=ws_cuffdiff_id,
    labels=['inactive'],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
