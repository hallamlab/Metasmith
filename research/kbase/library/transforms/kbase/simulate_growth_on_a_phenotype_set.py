# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""simulate_growth_on_a_phenotype_set -- Simulate Growth on Phenotype Data

KBase app   : simulate_growth_on_a_phenotype_set (spec 1.0.0, inactive)
module      : none -- legacy spec, no catalog module
git         : - @ -
service     : KBaseFBAModeling.simulate_phenotypes

Use Flux Balance Analysis (FBA) to simulate multiple growth phenotypes.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study           = model.AddRequirement(lib.GetType("kbase::study"))
sample          = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
input_model     = model.AddRequirement(lib.GetType("kbase::KBaseFBA_FBAModel"), parents={sample})
input_phenoset  = model.AddRequirement(lib.GetType("kbase::KBasePhenotypes_PhenotypeSet"), parents={sample})
output_phenosim = model.AddProduct(lib.GetType("kbase::KBasePhenotypes_PhenotypeSimulationSet"))

def protocol(context: ExecutionContext):
    made = {output_phenosim: context.Output(output_phenosim)}
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=input_model,
    labels=['inactive'],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
