# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""MetagenomeUtils/edit_contigs_in_binned_contigs -- Modify Contigs in BinnedContigs - v1.0.2

KBase app   : MetagenomeUtils/edit_contigs_in_binned_contigs (spec 1.1.1, inactive)
module      : MetagenomeUtils
git         : https://github.com/kbaseapps/MetagenomeUtils @ 49489d11583772d669f84e61e3cca1ddd9973c41
service     : MetagenomeUtils.edit_bins_from_binned_contig

Add or remove specific contigs by ID in bins in BinnedContigs data

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study                     = model.AddRequirement(lib.GetType("kbase::study"))
sample                    = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
binned_contig_ref         = model.AddRequirement(lib.GetType("kbase::KBaseMetagenomes_BinnedContigs"), parents={sample})
env                       = model.AddRequirement(lib.GetType("env::MetagenomeUtils.env"))
output_binned_contig_name = model.AddProduct(lib.GetType("kbase::KBaseMetagenomes_BinnedContigs"))

def protocol(context: ExecutionContext):
    made = {output_binned_contig_name: context.Output(output_binned_contig_name)}
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=binned_contig_ref,
    labels=['inactive'],
    resources=Resources(
        cpus=16,
        memory=Size.GB(32),
        duration=Duration(hours=24),
    ),
)
