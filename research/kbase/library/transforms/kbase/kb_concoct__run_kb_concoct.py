# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""kb_concoct/run_kb_concoct -- Bin Contigs using CONCOCT - v1.1

KBase app   : kb_concoct/run_kb_concoct (spec 1.3.4, active)
module      : kb_concoct
git         : https://github.com/kbaseapps/kb_concoct @ 936c9bfed7e07dd1ddcb76ac4fa775b73d60afbb
service     : kb_concoct.run_kb_concoct

Group assembled metagenomic contigs into lineages (Bins) using depth-of-
coverage and nucleotide composition

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study              = model.AddRequirement(lib.GetType("kbase::study"))
sample             = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
assembly_ref       = model.AddRequirement(lib.GetType("kbase::accepts_34"), parents={sample})
reads_list         = model.AddRequirement(lib.GetType("kbase::accepts_05"), parents={sample})
env                = model.AddRequirement(lib.GetType("env::kb_concoct.env"))
binned_contig_name = model.AddProduct(lib.GetType("kbase::KBaseMetagenomes_BinnedContigs"))
report_            = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {binned_contig_name: context.Output(binned_contig_name)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=assembly_ref,
    labels=[],
    resources=Resources(
        cpus=16,
        memory=Size.GB(32),
        duration=Duration(hours=24),
    ),
)
