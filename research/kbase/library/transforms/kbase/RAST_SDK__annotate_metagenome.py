# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""RAST_SDK/annotate_metagenome -- Annotate Metagenome Assembly and Re-annotate Metagenome with RASTtk - v1.073

KBase app   : RAST_SDK/annotate_metagenome (spec 1.9.5, active)
module      : RAST_SDK
git         : https://github.com/kbaseapps/RAST_SDK @ 7171090d87fccc8b7ecf1a1d02398995dcc2dd45
service     : RAST_SDK.annotate_metagenome

Annotate Metagenome Assembly and Re-annotate Metagenome with RASTtk (Rapid
Annotations using Subsystems Technology toolkit).

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study                  = model.AddRequirement(lib.GetType("kbase::study"))
sample                 = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
object_ref             = model.AddRequirement(lib.GetType("kbase::accepts_44"), parents={sample})
env                    = model.AddRequirement(lib.GetType("env::RAST_SDK.env"))
output_metagenome_name = model.AddProduct(lib.GetType("kbase::KBaseMetagenomes_AnnotatedMetagenomeAssembly"))
report_                = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output_metagenome_name: context.Output(output_metagenome_name)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=object_ref,
    labels=[],
    resources=Resources(
        cpus=8,
        memory=Size.GB(16),
        duration=Duration(hours=12),
    ),
)
