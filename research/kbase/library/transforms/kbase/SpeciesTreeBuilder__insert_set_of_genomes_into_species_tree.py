# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""SpeciesTreeBuilder/insert_set_of_genomes_into_species_tree -- Insert Genome Into SpeciesTree - v2.2.0

KBase app   : SpeciesTreeBuilder/insert_set_of_genomes_into_species_tree (spec 0.1.4, active)
module      : SpeciesTreeBuilder
git         : https://github.com/kbaseapps/SpeciesTreeBuilder @ dce166f6d1673018a001b750c191b9a2deda0c71
service     : SpeciesTreeBuilder.construct_species_tree

Add one or more Genomes to a KBase SpeciesTree.

References workspace types the registry no longer serves:
KBaseGenomeAnnotations.GenomeAnnotation. They are still declared, from the app
spec's own naming.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study         = model.AddRequirement(lib.GetType("kbase::study"))
sample        = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
param0        = model.AddRequirement(lib.GetType("kbase::accepts_47"), parents={sample})
env           = model.AddRequirement(lib.GetType("env::SpeciesTreeBuilder.env"))
treeid        = model.AddProduct(lib.GetType("kbase::KBaseTrees_Tree"))
genomesetname = model.AddProduct(lib.GetType("kbase::KBaseSearch_GenomeSet"))
report_       = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {treeid: context.Output(treeid)}
    made[genomesetname] = context.Output(genomesetname)
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=param0,
    labels=[],
    resources=Resources(
        cpus=8,
        memory=Size.GB(16),
        duration=Duration(hours=8),
    ),
)
