# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""KBaseRNASeq/describe_rnaseq_experiment -- Create RNA-seq Sample Set

KBase app   : KBaseRNASeq/describe_rnaseq_experiment (spec 1.0.5, inactive)
module      : KBaseRNASeq
git         : https://github.com/kbase/KBaseRNASeq @ 07a2978aea7cabf4decec09c2dc51be81e26f02d
service     : KBaseRNASeq.CreateRNASeqSampleSet

Provide RNA-seq reads and the metadata to create an RNA-seq Sample Set

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study        = model.AddRequirement(lib.GetType("kbase::study"))
sample       = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
sample_id    = model.AddRequirement(lib.GetType("kbase::accepts_05"), parents={sample})
env          = model.AddRequirement(lib.GetType("env::KBaseRNASeq.env"))
sampleset_id = model.AddProduct(lib.GetType("kbase::KBaseRNASeq_RNASeqSampleSet"))

def protocol(context: ExecutionContext):
    made = {sampleset_id: context.Output(sampleset_id)}
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=sample_id,
    labels=['inactive'],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
