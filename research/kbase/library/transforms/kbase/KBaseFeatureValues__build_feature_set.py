# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""KBaseFeatureValues/build_feature_set -- Build Feature Set from Genome

KBase app   : KBaseFeatureValues/build_feature_set (spec 0.0.22, inactive)
module      : KBaseFeatureValues
git         : https://github.com/kbaseapps/FeatureValues @ 6cdc50905a08883a53333c073abe1e1df7b3f97f
service     : KBaseFeatureValues.build_feature_set

Create a new Feature Set by selecting features from a Genome.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study              = model.AddRequirement(lib.GetType("kbase::study"))
sample             = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
input_genome       = model.AddRequirement(lib.GetType("kbase::KBaseGenomes_Genome"), parents={sample})
base_feature_set   = model.AddRequirement(lib.GetType("kbase::KBaseCollections_FeatureSet"), parents={sample})  # optional in KBase
env                = model.AddRequirement(lib.GetType("env::KBaseFeatureValues.env"))
output_feature_set = model.AddProduct(lib.GetType("kbase::KBaseCollections_FeatureSet"))

def protocol(context: ExecutionContext):
    made = {output_feature_set: context.Output(output_feature_set)}
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=input_genome,
    labels=['inactive'],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
