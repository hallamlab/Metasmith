# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""kb_hmmer/HMMER_MSA_Search -- HMMER Search from MSA (prot-prot) - v3.3.2

KBase app   : kb_hmmer/HMMER_MSA_Search (spec 1.9.1, active)
module      : kb_hmmer
git         : https://github.com/kbaseapps/kb_hmmer @ 6c338791492e2980534a08a1606d2d7137884759
service     : kb_hmmer.HMMER_MSA_Search

Search for matches to a Multiple Sequence Alignment (MSA) using Hidden Markov
Model Search (hmmsearch).

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study                = model.AddRequirement(lib.GetType("kbase::study"))
sample               = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
input_msa_ref        = model.AddRequirement(lib.GetType("kbase::KBaseTrees_MSA"), parents={sample})
input_many_ref       = model.AddRequirement(lib.GetType("kbase::accepts_19"), parents={sample})
env                  = model.AddRequirement(lib.GetType("env::kb_hmmer.env"))
output_filtered_name = model.AddProduct(lib.GetType("kbase::KBaseCollections_FeatureSet"))
report_              = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output_filtered_name: context.Output(output_filtered_name)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=input_msa_ref,
    labels=[],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
