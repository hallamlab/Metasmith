# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""FBAFileUtil/import_media_excel_file -- MEDIA FROM EXCEL FILE

KBase app   : FBAFileUtil/import_media_excel_file (spec 0.1.1, inactive)
module      : FBAFileUtil
git         : https://github.com/kbaseapps/FBAFileUtil @ d07004d2b3710a43dbb1c17a0726d9a22ab35dce
service     : FBAFileUtil.excel_file_to_media

none

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study        = model.AddRequirement(lib.GetType("kbase::study"))
sample       = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
env          = model.AddRequirement(lib.GetType("env::FBAFileUtil.env"))
outputobject = model.AddProduct(lib.GetType("kbase::KBaseBiochem_Media"))

def protocol(context: ExecutionContext):
    made = {outputobject: context.Output(outputobject)}
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=sample,
    labels=['inactive'],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
