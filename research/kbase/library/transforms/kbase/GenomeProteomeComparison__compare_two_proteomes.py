# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""GenomeProteomeComparison/compare_two_proteomes -- Compare Two Proteomes

KBase app   : GenomeProteomeComparison/compare_two_proteomes (spec 0.0.8, active)
module      : GenomeProteomeComparison
git         : https://github.com/kbaseapps/GenomeProteomeComparison @ f96f62941820a793124a18fe6e655da33a562033
service     : GenomeProteomeComparison.blast_proteomes

Compute bi-directional-best-hits between the proteins present in two input
Genomes. Produces a dot plot matrix showing corresponding genes in two
Genomes, as well as a table of gene differences.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study  = model.AddRequirement(lib.GetType("kbase::study"))
sample = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
param0 = model.AddRequirement(lib.GetType("kbase::KBaseGenomes_Genome"), parents={sample})
param1 = model.AddRequirement(lib.GetType("kbase::KBaseGenomes_Genome"), parents={sample})
env    = model.AddRequirement(lib.GetType("env::GenomeProteomeComparison.env"))
param2 = model.AddProduct(lib.GetType("kbase::GenomeComparison_ProteomeComparison"))

def protocol(context: ExecutionContext):
    made = {param2: context.Output(param2)}
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=param0,
    labels=[],
    resources=Resources(
        cpus=8,
        memory=Size.GB(16),
        duration=Duration(hours=8),
    ),
)
