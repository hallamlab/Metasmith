# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""FamaProfiling/run_FamaGenomeProfiling -- Run Fama Genome Profiling - v1.1

KBase app   : FamaProfiling/run_FamaGenomeProfiling (spec 1.1.0, active)
module      : FamaProfiling
git         : https://github.com/aekazakov/FamaProfiling @ d9db15ea217e3be2aab65c356564a6d345b4f410
service     : FamaProfiling.run_FamaGenomeProfiling

Generate a functional profile of genomes with Fama

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study                   = model.AddRequirement(lib.GetType("kbase::study"))
sample                  = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
genome_ref              = model.AddRequirement(lib.GetType("kbase::accepts_53"), parents={sample})
env                     = model.AddRequirement(lib.GetType("env::FamaProfiling.env"))
output_feature_set_name = model.AddProduct(lib.GetType("kbase::KBaseCollections_FeatureSet"))
output_annotation_name  = model.AddProduct(lib.GetType("kbase::KBaseGeneFamilies_DomainAnnotation"))
report_                 = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output_feature_set_name: context.Output(output_feature_set_name)}
    made[output_annotation_name] = context.Output(output_annotation_name)
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=genome_ref,
    labels=[],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
