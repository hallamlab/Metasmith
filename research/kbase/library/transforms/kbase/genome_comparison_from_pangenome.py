# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""genome_comparison_from_pangenome -- Compare Genomes from Pangenome

KBase app   : genome_comparison_from_pangenome (spec 1.0.0, inactive)
module      : none -- legacy spec, no catalog module
git         : - @ -
service     : KBaseFBAModeling.compare_genomes

Compare isofunctional and homologous gene families for all genomes in the
pangenome.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study           = model.AddRequirement(lib.GetType("kbase::study"))
sample          = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
input_pangenome = model.AddRequirement(lib.GetType("kbase::KBaseGenomes_Pangenome"), parents={sample})
output_gencomp  = model.AddProduct(lib.GetType("kbase::KBaseGenomes_GenomeComparison"))

def protocol(context: ExecutionContext):
    made = {output_gencomp: context.Output(output_gencomp)}
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=input_pangenome,
    labels=['inactive'],
    resources=Resources(
        cpus=8,
        memory=Size.GB(16),
        duration=Duration(hours=8),
    ),
)
