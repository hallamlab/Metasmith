# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""CoExpression/expression_toolkit_cluster_WGCNA -- Cluster Expression Data - WGCNA

KBase app   : CoExpression/expression_toolkit_cluster_WGCNA (spec 1.2.2, active)
module      : CoExpression
git         : https://github.com/sjyoo/coexpression @ 77803912cdb5fb1ed2535add11ccaa04bcb678f3
service     : CoExpression.const_coex_net_clust

Perform weighted gene co-expression network analysis (WGCNA) to detect gene
clusters and expression patterns.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study                   = model.AddRequirement(lib.GetType("kbase::study"))
sample                  = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
input_expression_matrix = model.AddRequirement(lib.GetType("kbase::KBaseFeatureValues_ExpressionMatrix"), parents={sample})
env                     = model.AddRequirement(lib.GetType("env::CoExpression.env"))
output_cluster_set      = model.AddProduct(lib.GetType("kbase::KBaseFeatureValues_FeatureClusters"))

def protocol(context: ExecutionContext):
    made = {output_cluster_set: context.Output(output_cluster_set)}
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=input_expression_matrix,
    labels=[],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
