# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""kb_trimmomatic/run_trimmomatic -- Trim Reads with Trimmomatic - v0.39

KBase app   : kb_trimmomatic/run_trimmomatic (spec 1.2.15, active)
module      : kb_trimmomatic
git         : https://github.com/kbaseapps/kb_trimmomatic @ 5fb0fc6aa22ac62359f5bd8f0d407f51c99beea5
service     : kb_trimmomatic.runTrimmomatic

Trim paired- or single-end Illumina reads with Trimmomatic.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study             = model.AddRequirement(lib.GetType("kbase::study"))
sample            = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
input_reads_ref   = model.AddRequirement(lib.GetType("kbase::accepts_10"), parents={sample})
env               = model.AddRequirement(lib.GetType("env::kb_trimmomatic.env"))
# output_reads_name is one of 3 types; each is its own product branch
output_reads_name = model.AddProduct(lib.GetType("kbase::KBaseFile_PairedEndLibrary"))
model.NewProductGroup()
output_reads_name_2 = model.AddProduct(lib.GetType("kbase::KBaseFile_SingleEndLibrary"))
model.NewProductGroup()
output_reads_name_3 = model.AddProduct(lib.GetType("kbase::KBaseSets_ReadsSet"))
report_           = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output_reads_name: context.Output(output_reads_name)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=input_reads_ref,
    labels=[],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=6),
    ),
)
