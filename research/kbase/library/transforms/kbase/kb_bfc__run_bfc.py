# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""kb_bfc/run_bfc -- BFC - Bloom Filter Read Error Correction

KBase app   : kb_bfc/run_bfc (spec 0.0.2, active)
module      : kb_bfc
git         : https://github.com/psdehal/kb_bfc @ bd4c653197f1b899067694dc18220b6a05734273
service     : kb_bfc.run_bfc

Error correction for short illumina reads

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study             = model.AddRequirement(lib.GetType("kbase::study"))
sample            = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
input_reads_upa   = model.AddRequirement(lib.GetType("kbase::accepts_12"), parents={sample})
env               = model.AddRequirement(lib.GetType("env::kb_bfc.env"))
# output_reads_name is one of 2 types; each is its own product branch
output_reads_name = model.AddProduct(lib.GetType("kbase::KBaseAssembly_PairedEndLibrary"))
model.NewProductGroup()
output_reads_name_2 = model.AddProduct(lib.GetType("kbase::KBaseFile_PairedEndLibrary"))
report_           = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output_reads_name: context.Output(output_reads_name)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=input_reads_upa,
    labels=[],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=6),
    ),
)
