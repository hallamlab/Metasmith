# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""kb_gtdbtk/run_kb_gtdbtk_classify_wf -- Classify Microbes with GTDB-Tk - v2.3.2

KBase app   : kb_gtdbtk/run_kb_gtdbtk_classify_wf (spec 1.4.1, active)
module      : kb_gtdbtk
git         : https://github.com/kbaseapps/kb_gtdbtk @ 99839a03cebee1110c6cd6d807dc448821837580
service     : kb_gtdbtk.run_kb_gtdbtk_classify_wf

Obtain objective taxonomic assignments for bacterial and archaeal genomes
based on the Genome Taxonomy Database (GTDB)

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study                = model.AddRequirement(lib.GetType("kbase::study"))
sample               = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
input_object_ref     = model.AddRequirement(lib.GetType("kbase::accepts_59"), parents={sample})
env                  = model.AddRequirement(lib.GetType("env::kb_gtdbtk.env"))
output_tree_basename = model.AddProduct(lib.GetType("kbase::KBaseTrees_Tree"))
report_              = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output_tree_basename: context.Output(output_tree_basename)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=input_object_ref,
    labels=[],
    resources=Resources(
        cpus=8,
        memory=Size.GB(16),
        duration=Duration(hours=8),
    ),
)
