# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""expression_toolkit_filter_expression -- Identify Differentially Expressed Genes

KBase app   : expression_toolkit_filter_expression (spec 1.0.0, inactive)
module      : none -- legacy spec, no catalog module
git         : - @ -
service     : CoExpression.coex_filter

Filter an Expression Matrix to show differentially expressed genes.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study               = model.AddRequirement(lib.GetType("kbase::study"))
sample              = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
expression          = model.AddRequirement(lib.GetType("kbase::KBaseFeatureValues_ExpressionMatrix"), parents={sample})
filtered_expression = model.AddProduct(lib.GetType("kbase::KBaseFeatureValues_ExpressionMatrix"))
filtered_featureset = model.AddProduct(lib.GetType("kbase::KBaseCollections_FeatureSet"))

def protocol(context: ExecutionContext):
    made = {filtered_expression: context.Output(filtered_expression)}
    made[filtered_featureset] = context.Output(filtered_featureset)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=expression,
    labels=['inactive'],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
