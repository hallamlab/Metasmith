# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""kb_djornl/run_rwr_cv -- Find Gene Set Interconnectivity using Cross Validation with RWRtools CV

KBase app   : kb_djornl/run_rwr_cv (spec 1.0.3, active)
module      : kb_djornl
git         : https://github.com/kbaseapps/kb_djornl/ @ 3b136023d9f6fa7dfb6e18af94bc362e5ca57809
service     : kb_djornl.run_rwr_cv

RWRtools CV (Cross Validation) performs cross validation on a single gene set,
finding the RWR rank of the left-out genes.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study             = model.AddRequirement(lib.GetType("kbase::study"))
sample            = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
seeds_feature_set = model.AddRequirement(lib.GetType("kbase::KBaseCollections_FeatureSet"), parents={sample})
env               = model.AddRequirement(lib.GetType("env::kb_djornl.env"))
output_name       = model.AddProduct(lib.GetType("kbase::KBaseCollections_FeatureSet"))
report_           = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output_name: context.Output(output_name)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=seeds_feature_set,
    labels=[],
    resources=Resources(
        cpus=8,
        memory=Size.GB(16),
        duration=Duration(hours=8),
    ),
)
