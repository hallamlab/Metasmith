# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""fba_tools/gapfill_metabolic_model -- Gapfill Metabolic Model

KBase app   : fba_tools/gapfill_metabolic_model (spec 2.2.1, active)
module      : fba_tools
git         : https://github.com/kbaseapps/fba_tools @ b083384ac00d4f9d7cb796a664ee3ffd017cf248
service     : fba_tools.gapfill_metabolic_model

Identify the minimal set of biochemical reactions to add to a draft metabolic
model to enable it to produce biomass in a specified media.<b>This app is now
obsolete, replaced by the new ModelSEED2 app: <i>MS2 - Improved Gapfill
Metabolic Models</i>.</b>

Optional in KBase and therefore not a requirement here: media_id,
source_fbamodel_id.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study              = model.AddRequirement(lib.GetType("kbase::study"))
sample             = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
fbamodel_id        = model.AddRequirement(lib.GetType("kbase::KBaseFBA_FBAModel"), parents={sample})
env                = model.AddRequirement(lib.GetType("env::fba_tools.env"))
fbamodel_output_id = model.AddProduct(lib.GetType("kbase::KBaseFBA_FBAModel"))
report_            = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {fbamodel_output_id: context.Output(fbamodel_output_id)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=fbamodel_id,
    labels=[],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
