# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""legacy_reads_conversion/legacy_reads_conversion -- Convert Legacy Read Library to New Read Library

KBase app   : legacy_reads_conversion/legacy_reads_conversion (spec 0.0.2, active)
module      : legacy_reads_conversion
git         : https://github.com/kbaseapps/legacy_reads_conversion @ 2c071c5ae32e83a94a1fc4980ac223deefb54fe4
service     : legacy_reads_conversion.run_legacy_reads_conversion

Convert legacy read library objects types to the latest version of read
library object types

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study             = model.AddRequirement(lib.GetType("kbase::study"))
sample            = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
read_library_name = model.AddRequirement(lib.GetType("kbase::accepts_04"), parents={sample})
env               = model.AddRequirement(lib.GetType("env::legacy_reads_conversion.env"))
# output is one of 2 types; each is its own product branch
output            = model.AddProduct(lib.GetType("kbase::KBaseFile_PairedEndLibrary"))
model.NewProductGroup()
output_2          = model.AddProduct(lib.GetType("kbase::KBaseFile_SingleEndLibrary"))
report_           = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output: context.Output(output)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=read_library_name,
    labels=[],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
