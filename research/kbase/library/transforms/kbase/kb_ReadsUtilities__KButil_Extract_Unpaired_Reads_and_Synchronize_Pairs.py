# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""kb_ReadsUtilities/KButil_Extract_Unpaired_Reads_and_Synchronize_Pairs -- Extract Unpaired Reads and Synchronize Pairs - v1.0.1

KBase app   : kb_ReadsUtilities/KButil_Extract_Unpaired_Reads_and_Synchronize_Pairs (spec 1.2.2, inactive)
module      : kb_ReadsUtilities
git         : https://github.com/kbaseapps/kb_ReadsUtilities @ de8955d0a9f373bff40882a02cd7742aa7560eea
service     : kb_ReadsUtilities.KButil_Extract_Unpaired_Reads_and_Synchronize_Pairs

Extract unpaired reads from PairedEnd reads libraries or reads sets and match
the order of paired reads

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study       = model.AddRequirement(lib.GetType("kbase::study"))
sample      = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
input_ref   = model.AddRequirement(lib.GetType("kbase::accepts_31"), parents={sample})
env         = model.AddRequirement(lib.GetType("env::kb_ReadsUtilities.env"))
# output_name is one of 2 types; each is its own product branch
output_name = model.AddProduct(lib.GetType("kbase::KBaseFile_PairedEndLibrary"))
model.NewProductGroup()
output_name_2 = model.AddProduct(lib.GetType("kbase::KBaseSets_ReadsSet"))
report_     = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output_name: context.Output(output_name)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=input_ref,
    labels=['inactive'],
    resources=Resources(
        cpus=2,
        memory=Size.GB(4),
        duration=Duration(hours=2),
    ),
)
