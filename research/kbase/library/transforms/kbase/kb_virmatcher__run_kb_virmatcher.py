# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""kb_virmatcher/run_kb_virmatcher -- VirMatcher 0.3.3

KBase app   : kb_virmatcher/run_kb_virmatcher (spec 1.0.0, active)
module      : kb_virmatcher
git         : https://github.com/bolduc/kb_virmatcher.git @ d933e4dc9a98ed5fe38517802155765bfc85d79b
service     : kb_virmatcher.run_kb_virmatcher

Predicts host-virus matches

References workspace types the registry no longer serves:
KBaseMetagenomes.Genomes. They are still declared, from the app spec's own
naming.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study         = model.AddRequirement(lib.GetType("kbase::study"))
sample        = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
viral_genomes = model.AddRequirement(lib.GetType("kbase::KBaseGenomeAnnotations_Assembly"), parents={sample})
host_genomes  = model.AddRequirement(lib.GetType("kbase::accepts_45"), parents={sample})
env           = model.AddRequirement(lib.GetType("env::kb_virmatcher.env"))
report_       = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {report_: context.Output(report_)}
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=viral_genomes,
    labels=[],
    resources=Resources(
        cpus=4,
        memory=Size.GB(8),
        duration=Duration(hours=4),
    ),
)
