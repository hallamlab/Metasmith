# GENERATED by research/kbase/library/_generate.py -- do not edit by hand.
"""MergeMetabolicAnnotations/import_bulk_annotations -- Bulk Import Annotations from Staging

KBase app   : MergeMetabolicAnnotations/import_bulk_annotations (spec 1.2.11, active)
module      : MergeMetabolicAnnotations
git         : https://github.com/jeffkimbrel/MergeMetabolicAnnotations @ ec971d114d57942cef73dc2980c8faf48cea7afe
service     : MergeMetabolicAnnotations.import_bulk_annotations

Import a file in TSV format from your staging area with new annotations to add
to an existing genome

Optional in KBase and therefore not a requirement here: genome.

Stub: the model is the port, the body only touches its outputs. This does
not run the KBase app. Regenerate with research/kbase/library/_generate.py.
"""

from metasmith.python_api import *

lib   = TransformInstanceLibrary.ResolveParentLibrary(__file__)
model = Transform()
study       = model.AddRequirement(lib.GetType("kbase::study"))
sample      = model.AddRequirement(lib.GetType("kbase::sample"), parents={study})
env         = model.AddRequirement(lib.GetType("env::MergeMetabolicAnnotations.env"))
output_name = model.AddProduct(lib.GetType("kbase::KBaseGenomes_Genome"))
report_     = model.AddProduct(lib.GetType("kbase::report"))

def protocol(context: ExecutionContext):
    made = {output_name: context.Output(output_name)}
    made[report_] = context.Output(report_)
    for path in made.values():
        context.external_shell.Exec(f'touch {path.external}')
    return ExecutionResult(
        manifest=[{k: v.local for k, v in made.items()}],
        success=all(v.local.exists() for v in made.values()),
    )

TransformInstance(
    protocol=protocol,
    model=model,
    group_by=sample,
    labels=[],
    resources=Resources(
        cpus=8,
        memory=Size.GB(16),
        duration=Duration(hours=12),
    ),
)
