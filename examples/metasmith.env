# A generic env declaration: the same tool slot, described for both worlds.
# `bash`/`echo`/`cat` are what these example transforms actually need, so the
# conda side names `base` -- present in every conda/mamba install, which keeps
# the example library host-agnostic.
container: docker://quay.io/hallamlab/metasmith:latest
conda: base
