channels:
  - bioconda
  - conda-forge
dependencies:
  - python=3.12
  - nextflow=26.04.1
  - graphviz=13.1.1 # `neato -n2` rasterizes raster DAG formats; layout is ours
  - pyyaml=6.0.2
  - numpy=2.2.5
  - rsync=3.4.1
  - pigz=2.8
  - unzip=6.0
  - git=2.52.0 # tutorials and user libraries; the standard library is a package

  # The standard library, and the reason `msm gui` has anything in its type
  # panel: `gui/stdlib.clone_stdlib` locates this package by import, copies it
  # into the project and compiles it there. Without it metasmith installs and
  # runs, and every page is empty.
  #
  # The dependency is one-way ON PURPOSE. metasmith_libraries used to declare
  # `metasmith` in turn -- true of what imports its transforms, but a cycle
  # conda cannot solve, and wrong besides: the library is content, with no
  # entry point and nothing to run. The engine that reads it is the consumer.
  - metasmith_libraries
  - tk=8.6.13 # for globus

  # # relay now just uses files
  # # networking (mostly for relay)
  # - python-socketio=5.14.2
  # - python-engineio=4.12.3
  # - websocket-client=1.9.0
  # - requests=2.32.3
  # - aiohttp=3.13.1
  
  # UI: `msm gui` serves a prebuilt bundle, so only the server side is needed
  # here — node is a build dependency, not a runtime one.
  - flask=3.1.3
  - coolname=2.2.0 # readable workflow names: blazing-ape, crystalline-lake

  # UI via jupyterlab
  - jupyterlab=4.5.0
  - jupyterlab-lsp=5.2.0
  - python-lsp-server=1.13.2
  - jupyterlab-unfold=0.3.4
  - theme-material-darcula=1.0.3

  # common tools (not strictly dependencies)
  - pandas=2.3.3
  - openpyxl=3.1.5  # for pandas to read excel files
  # parquet, which is the fabfos lane's format end to end -- the GPR tables, the
  # bake, and every ecspr product. Without it `pytest tests/fabfos` does not
  # collect, because its tests import the research drivers that read them.
  - pyarrow=25.0.0
  - plotly=6.5.0
  - scipy=1.16.3
  
  # globus
  - globus-cli=3.40.0
  - globus-sdk=4.1.0

  # caching: canonical CBOR encoding + blake3 multihash keys
  - cbor2=5.6.5
  - blake3=0.4.1

# there can NOT be any pip dependencies,
# since those will not be installed with the conda package
