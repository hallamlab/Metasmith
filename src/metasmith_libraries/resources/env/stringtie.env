# stringtie 2.2.3, biocontainers tag 2.2.3--h29c0135_1
# (sha256:84207eb013dbcff90928d8ddc75902580d3885ce8edaa36b8fab7665a3dc2e3c,
# verified 2026-09-04: `stringtie --version` reports 2.2.3).
# The tag this file carried before, 2.2.3--h43eeafb_1, no longer resolves on
# quay -- the pull fails outright rather than silently fetching something else.
# Same version, the build that is still published.
container: docker://quay.io/biocontainers/stringtie:2.2.3--h29c0135_1
conda: stringtie
