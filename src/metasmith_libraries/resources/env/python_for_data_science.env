# Digest of tag 1.2.5 (quay, 2026-06-03), which ships polars 1.41.2.
#
# NOT sha256:334b01e4... -- that is what the env migration pinned, and it resolves
# to tags `1.3.1` / `plotly_no_chrome` built 2025-12-16: an OLDER image than 1.2.5
# despite the higher version number, and one with no polars at all. Every consumer
# that reaches for polars in this image dies with ModuleNotFoundError after its
# expensive work has already succeeded -- centrifuger classified 96.76% of S5's
# reads, then failed converting the TSV to parquet. 80 tasks in run SINPiD1W went
# that way.
#
# Verified in both images before changing this: 1.2.5 has polars 1.41.2, the
# 334b01e4 digest has none.
container: docker://quay.io/hallamlab/python_for_data_science@sha256:7684b1bd22038de100f4438b2ae049ce7f507960c362d5d0c8495417318d4a9c
conda: python_for_data_science
