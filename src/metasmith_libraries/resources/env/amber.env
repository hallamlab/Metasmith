# 2.0.7--pyhdfd78af_0, verified 2026-09-11, digest
# sha256:389eb9638732691b33668a94ec2cdfa402d3b23adfd45663598a3bc8212e654e.
# Pulled and smoke-tested locally: `amber.py -g gold.tsv -l tool -o out --skip_gs
# pred.tsv` against a 5-contig/3-genome synthetic gold standard and prediction
# produced `out/results.tsv` and `out/genome/tool/metrics_per_bin.tsv` with the
# column sets the two amber.py transforms in this library read.
container: docker://quay.io/biocontainers/cami-amber:2.0.7--pyhdfd78af_0
conda: amber
