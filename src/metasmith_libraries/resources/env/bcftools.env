# bcftools 1.24, biocontainers tag 1.24--h118bc1c_2. Carries both `bcftools mpileup`
# and `bcftools call`, which is the whole variant-calling arm.
# Tag confirmed active on quay.io 2026-09-04 (digest present, onlyActiveTags).
container: docker://quay.io/biocontainers/bcftools:1.24--h118bc1c_2
conda: bcftools
