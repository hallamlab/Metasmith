# 17.b804f, verified 2026-09-09, digest
# sha256:912f11bbc736cdb5786378f81a1c3a617d140f19ee2e4f2ec55872db32c48273.
# `mmseqs version` answers 17.b804f; both clustering steps were run in this image
# against a pooled contig set and produced <prefix>_cluster.tsv and
# <prefix>_rep_seq.fasta. 18.8cc5c exists; the two steps differ only by
# --cov-mode, so a major bump is a behaviour question, not a packaging one.
container: docker://quay.io/biocontainers/mmseqs2:17.b804f--hd6d6fdc_1
conda: mmseqs2
