ontology:
  doi: https://doi.org/10.1093/bioinformatics/btt113
  name: EDAM
  strict: false
  version: '1.25'
schema: v1
types:
  fabfos_recovery.py:
    properties:
      _:
      - insert recovery -- `map` states where the pCC1fos backbone lands and interprets
        nothing, `rectify` cuts at those footprints with the assembly graph beside
        it (circularity is the assembler's own claim; closure is a count in {0,1,2}),
        `dedup` collapses the pieces across assemblers and pools into the representative
        insert set
      src: fabfos_recovery
      usage: command line
